avg = []
while True:
    count = input("-->")
    if count.isdigit() == True:
        if count in "0":
            break
        else:
            avg.append(int(count))
    else:
        print("Введите число")
      
print(f"средняя зарплата - {sum(avg)/len(avg)}")
