class Person:
    def __init__(self, *args):
        self.args = args
        self.str = "".join(args)
    def show(self):
        return self.str[::-1]

p = Person("Иванов", "Михаил", "Федорович")
print(p.show())
